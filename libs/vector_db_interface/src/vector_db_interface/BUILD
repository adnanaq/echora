"""Vector DB Interface sources - provider-agnostic interface definitions."""

python_sources(
    # Explicit: No dependencies on other internal libs (interface/contract definition)
    dependencies=[],
)
