"""Mappers — pure value normalization functions for all 7 enrichment sources.

Each mapper converts a source-specific scraped model into a dict of
canonical field values matching libs/common/src/common/models/anime.py.
"""
