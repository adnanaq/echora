"""Common package sources - base package with no internal lib dependencies."""

python_sources(
    name="common",
    sources=["**/*.py"],
    # Explicit: No dependencies on other internal libs (base package)
    dependencies=[],
)
